{
  "name": "dependabot-auto-comment-merge",
  "version": "1.0.0",
  "description": "GitHub Action to automatically comment @dependabot merge on open Dependabot PRs",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github",
    "action",
    "dependabot",
    "automation"
  ],
  "author": "gesslar",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
